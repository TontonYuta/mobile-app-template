import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tontonyuta.aofmath',
  appName: 'AOF Math Tutor AI',
  webDir: 'dist'
};

export default config;
